* what does ${foo} mean inside template literals? should we treat it as a thing?

* left side of "for" allows declarations (but nothing else), yield keyword
==> keywords are accepted everywhere here but just invalid